spring:
  application:
    name: product-service
  cloud:
    vault:
      uri: http://localhost:8200
      authentication: TOKEN
      token: ${VAULT_TOKEN}
      kv:
        enabled: true
  config:
    import: vault://secret/product-service,vault://secret/global


